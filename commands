docker

docker ps
docker ps -a
doxker image

docker build -t <imagename> <imagedockerfileder>
docker build <dockerfiledire> -t <name-of-image> -f <name-of-dockerfile>
docker build <dockerfiledire> -t <name-of-image>
docker build <dockerfiledire> -t <name-of-image>:<version>
docker image history <imagename>
docker run -p <host-port>:<container-port> <name-of-container>
docker run <name-of-container>:<version>
docker stop <imagename>

docker login --username <login>
docker image tag helloworld <login>/<imagename>
docker rm <containerID>
docker image push <login>/<imagename>

docker pull <imagenamefromdockerregistry>

docker-compose up &
docker compose -f <adormodifiething> up

docker run -it <imagename> <whattorun>
docker rm $(docker ps -a -q -f status=exited)
docker container prune